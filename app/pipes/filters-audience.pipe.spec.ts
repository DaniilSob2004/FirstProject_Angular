import { FiltersAudiencePipe } from './filters-audience.pipe';

describe('FiltersAudiencePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersAudiencePipe();
    expect(pipe).toBeTruthy();
  });
});
